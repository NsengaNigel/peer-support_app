import 'package:flutter/material.dart';

class SavedPostsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Saved Posts')),
      body: Center(child: Text('Your Saved Posts')),
    );
  }
} 